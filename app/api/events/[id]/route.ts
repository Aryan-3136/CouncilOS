import { databaseError, supabase } from "../../../../db/supabase";
import { eventSchema, eventUpdate, serializeEvents } from "../shared";
export const dynamic = "force-dynamic";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const parsed = eventSchema.safeParse(await request.json()); if (!parsed.success) return Response.json({ error: parsed.error.issues[0]?.message }, { status: 400 }); const result = await eventUpdate((await params).id, parsed.data); return Response.json({ event: (await serializeEvents([result.data], result.client))[0] }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Unable to update event" }, { status: 500 }); } }
export async function DELETE(_: Request, { params }: { params: Promise<{ id: string }> }) { try { const { error } = await supabase().from("events").update({ archived: true }).eq("id", (await params).id).eq("archived", false); if (error) throw databaseError(error, "Unable to delete event"); return new Response(null, { status: 204 }); } catch (error) { return Response.json({ error: error instanceof Error ? error.message : "Unable to delete event" }, { status: 500 }); } }
