import { z } from "zod"; import { databaseError, supabase } from "../../../db/supabase"; export const dynamic="force-dynamic";
const schema=z.object({title:z.string().trim().min(1).max(200),description:z.string().trim().max(5000).default(""),area:z.string().trim().max(100).default("personal"),timeframe:z.string().trim().max(200).default(""),target:z.string().trim().max(500).default(""),progress:z.coerce.number().int().min(0).max(100).default(0),status:z.enum(["active","completed","paused","archived"]).default("active"),milestones:z.string().trim().max(10000).default(""),notes:z.string().trim().max(10000).default("")}); export type GoalInput=z.infer<typeof schema>; export function serializeGoal(r:Record<string,unknown>){return{id:String(r.id),title:String(r.title),description:String(r.description??""),area:String(r.area),timeframe:String(r.timeframe??""),target:String(r.target??""),progress:Number(r.progress??0),status:String(r.status),milestones:String(r.milestones??""),notes:String(r.notes??""),createdAt:String(r.created_at),updatedAt:String(r.updated_at)}}; export async function GET(){try{const{data,error}=await supabase().from("goals").select("*").eq("archived",false).order("updated_at",{ascending:false});if(error)throw databaseError(error,"Unable to load goals");return Response.json({goals:(data??[]).map(r=>serializeGoal(r as Record<string,unknown>))})}catch(e){return Response.json({error:e instanceof Error?e.message:"Unable to load goals"},{status:500})}} export async function POST(req:Request){try{const p=schema.safeParse(await req.json());if(!p.success)return Response.json({error:p.error.issues[0]?.message},{status:400});const{data,error}=await supabase().from("goals").insert(p.data).select().single();if(error)throw databaseError(error,"Unable to create goal");return Response.json({goal:serializeGoal(data as Record<string,unknown>)},{status:201})}catch(e){return Response.json({error:e instanceof Error?e.message:"Unable to create goal"},{status:500})}}
